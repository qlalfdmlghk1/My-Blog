# 의존성
node_modules

# Next.js
.next
out
next-env.d.ts
*.tsbuildinfo

# 환경 변수 — 서비스 계정 키가 들어가므로 절대 커밋 금지
.env
.env.local
.env*.local
!.env.example

# Vercel
.vercel

# OS / editor
.DS_Store
Thumbs.db

# 로그
*.log
npm-debug.log*

# 서비스 계정 키 — 다운로드한 JSON 이 저장소에 들어오지 않게 한다
firebase-adminsdk-*.json
service-account*.json
*-firebase-adminsdk-*.json

# 키 교체 시 생기는 백업 — 옛 키가 들어 있다
.env.local.bak
.env.*.bak
