# 기획 검수 — uncategorized-post-save

| 항목      | 값                    |
| --------- | --------------------- |
| 모드      | 기능 단위             |
| 검수일    | 2026-09-27            |
| 라운드 수 | 2 (수렴)              |
| 최대 회차 | 3 (기본값)            |

## 요약

- 🚫 개발 착수 불가: 0건 / ⚠️ 반드시 확인: 4건 / 💡 참고: 3건
- plan.md는 "검증 제거·빈 값 허용"만 적고 있지만, 실제 차단 지점이 **클라이언트 검증 + `firestore.rules`(보안 규칙) 두 곳**이고, 빈 카테고리 글이 **공개 화면에서 어떻게 보일지**가 정의되지 않았다.

## 검수 입력

| 소스                  | 확보 여부 | 비고                                                         |
| --------------------- | --------- | ------------------------------------------------------------ |
| plan.md               | ✅        | docs/features/uncategorized-post-save/plan.md                |
| docs/PLAN.md (기획 정본) | ✅     | 데이터 구조 절 — "글 1개당 카테고리 정확히 1개"              |
| 코드 기준선           | ✅        | PostEditor.tsx · firestore.rules · CategoryBadge.tsx · api/*  |
| Figma / Confluence    | 미확보    | 해당 없음 (1인 프로젝트)                                     |

## 라운드별 변경

- **Round 1**: 초기 5건 도출 (plan.md·PLAN.md·에디터 검증 대조)
- **Round 2**: 신규 +2건 (공개 화면·부가 경로 교차 검증 — 배지·커버 생성)
- **Round 3**: 미수행 — Round 2 이후 신규 관점 없음 → 수렴

## 🚫 개발 착수 불가 (0)

없음.

## ⚠️ 반드시 확인 (4)

#### 1. 기획 정본의 "카테고리 정확히 1개" 정책과 충돌

- **분류**: 정책 모순
- **근거**: `docs/PLAN.md:149` "글 1개당 카테고리 정확히 1개", 에디터 legend `카테고리 — 정확히 1개`(PostEditor.tsx:469), `types/post.ts` 주석
- **확인 질문**: 정책을 "0개 또는 1개"로 바꾸는 것이 맞나요? 맞다면 PLAN.md·에디터 문구·타입 주석도 이번 범위에 포함할까요?

#### 2. 초안만 허용인가, 발행 글도 허용인가

- **분류**: 미정의 케이스
- **근거**: plan.md "빈 카테고리로 저장 허용" — status(draft/published) 구분이 없음
- **확인 질문**: 카테고리 없이 **발행**까지 허용하나요, 아니면 **초안 저장**만 허용하고 발행할 때는 카테고리를 요구하나요?

#### 3. 보안 규칙(`firestore.rules`) 변경 필요 — 배포 포함 여부

- **분류**: 흐름 누락
- **근거**: `firestore.rules:37-38` `d.category.size() > 0 && categoryExists(...)` — 클라이언트 검증만 없애면 저장 시 permission 에러가 난다
- **확인 질문**: 규칙을 "빈 문자열이면 통과, 값이 있으면 exists()"로 완화하고 `firebase deploy --only firestore:rules`까지 이번 작업에 포함할까요? (권한 규칙 변경 → 사용자 확인 대상)

#### 4. 카테고리 없는 글의 공개 화면 표시

- **분류**: 미정의 케이스
- **근거**: `CategoryBadge`는 slug가 비면 **빈 배지**를 그린다. 카테고리 페이지·사이드바 카운트·revalidate는 카테고리별로만 경로를 만든다 → 이런 글은 홈·태그 목록에만 나온다
- **확인 질문**: 배지는 숨길까요, "카테고리 없음" 같은 문구로 보일까요? 공개 사이드바/카테고리 페이지에 "카테고리 없음" 묶음이 필요한가요, 아니면 홈·태그 목록에만 나와도 되나요?

## 💡 참고 (3)

#### 5. 에디터에서 "카테고리 없음"을 고르는 방법

- **근거**: 새 글은 첫 카테고리로 자동 선택된다(PostEditor.tsx:73). 라디오는 한 번 고르면 해제할 수 없다
- **확인 질문**: 라디오 목록에 "카테고리 없음" 항목을 추가하고, 새 글의 기본값을 무엇으로 둘까요? (첫 카테고리 유지 / 카테고리 없음)
- **권장 방향(참고, 사람 확인 필요)**: 소분류 목록의 "분류 없음" 항목처럼 "카테고리 없음" 라디오 추가

#### 6. 카테고리가 비었을 때 소분류

- **근거**: 소분류는 카테고리에 종속
- **확인 질문**: 카테고리가 비면 소분류도 빈 값으로 강제하는 것이 맞나요? (사실상 자명하나 명시)

#### 7. 커버 생성·OG 이미지

- **근거**: `api/cover/generate`는 카테고리가 없으면 palette null로 처리 — 이미 동작함. OG 카드도 무채색 fallback 여부만 확인하면 됨
- **확인 질문**: 무채색 fallback이면 충분한가요?

## 최종 확인 질문 목록

1. [⚠️] "카테고리 정확히 1개" 정책을 "0~1개"로 바꾸고 PLAN.md·문구도 고칠까요?
2. [⚠️] 카테고리 없이 발행까지 허용하나요, 초안만 허용하나요?
3. [⚠️] `firestore.rules` 완화 + 규칙 배포까지 이번 범위에 넣을까요?
4. [⚠️] 공개 화면에서 카테고리 없는 글의 배지·목록 노출을 어떻게 할까요?
5. [💡] 에디터에 "카테고리 없음" 라디오를 추가할까요? 새 글 기본값은?
6. [💡] 카테고리가 비면 소분류도 비우는 것이 맞나요?
7. [💡] 커버/OG는 무채색 fallback이면 충분한가요?

## 사람 결정 필요 여부

- **개발 착수 불가 0건 여부**: 예 → 다음 단계 진행 가능
- **사람이 반드시 결정해야 하는 정책 항목**: ⚠️ 1~4
- **AI가 임의로 확정한 정책**: 없음
